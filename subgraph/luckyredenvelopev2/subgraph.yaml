specVersion: 1.0.0
indexerHints:
  prune: auto
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum
    name: LuckyRedEnvelopeV2
    network: sepolia
    source:
      address: "0xE601515E7a2B7f7a964adbb4176ca54B08F9E03D"
      abi: LuckyRedEnvelopeV2
      startBlock: 5750413
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      entities:
        - ClaimPrize
        - DefaultAutoClaimChange
        - DefaultTokenChange
        - OperatorAddress
        - OwnershipTransferred
        - PrizeDrawn
        - RedEnvelopeClaimable
        - RedEnvelopeClosed
        - RedEnvelopeCreated
        - TicketsGet
        - TicketsInject
        - TicketsPurchase
      abis:
        - name: LuckyRedEnvelopeV2
          file: ./abis/LuckyRedEnvelopeV2.json
      eventHandlers:
        - event: ClaimPrize(indexed uint256,indexed address,uint256,bool)
          handler: handleClaimPrize
        - event: DefaultAutoClaimChange(bool)
          handler: handleDefaultAutoClaimChange
        - event: DefaultTokenChange(address,uint256)
          handler: handleDefaultTokenChange
        - event: OperatorAddress(address,bool)
          handler: handleOperatorAddress
        - event: OwnershipTransferred(indexed address,indexed address)
          handler: handleOwnershipTransferred
        - event: PrizeDrawn(indexed uint256,indexed address,indexed uint256,uint256,bool)
          handler: handlePrizeDrawn
        - event: RedEnvelopeClaimable(indexed uint256,uint256)
          handler: handleRedEnvelopeClaimable
        - event: RedEnvelopeClosed(indexed uint256,uint256,uint256,uint256,uint256)
          handler: handleRedEnvelopeClosed
        - event: RedEnvelopeCreated(indexed uint256,uint256,uint256,uint256,uint256,address,address,bool)
          handler: handleRedEnvelopeCreated
        - event: TicketsGet(indexed uint256,indexed address,indexed address,uint256,uint256)
          handler: handleTicketsGet
        - event: TicketsInject(indexed uint256,indexed address,uint256)
          handler: handleTicketsInject
        - event: TicketsPurchase(indexed uint256,indexed address,indexed address,uint256,uint256)
          handler: handleTicketsPurchase
      file: ./src/lucky-red-envelope-v-2.ts
